# Copy to .env and fill in for local overrides. Currently only used by
# settings/prod.py (not active yet). settings/dev.py works with zero
# environment variables.

DJANGO_SECRET_KEY=
DJANGO_ALLOWED_HOSTS=
DATABASE_NAME=
DATABASE_USER=
DATABASE_PASSWORD=
DATABASE_HOST=
DATABASE_PORT=
